name: Build & Deploy
on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:

      - name: Deploy Nodejs app
        uses: appleboy/ssh-action@v0.1.2
        with:
          host: ${{secrets.HOST}}
          key: ${{secrets.SSH_KEY}}
          username: ${{secrets.SSH_USER}}




          script: |

            mkdir projects

            cd clox

            git clone https://github.com/overlorde/clox.git

            echo 'Deployment Successful to artemis'
